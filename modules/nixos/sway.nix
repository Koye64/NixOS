{ ... }:

{
  programs.sway = {
    enable = true;
  };

  programs.waybar = {
    enable = true;
  };

  services.greetd = {
    enable = true;
  };

  programs.regreet = {
    enable = true;
  };
}
